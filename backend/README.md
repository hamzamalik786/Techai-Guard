# TechuAI Backend

Minimal backend scaffold. Contains an Express server, Prisma schema and modules for auth, AI provider adapters, guardrails and file processing.

Run in development:

1. Copy `.env.example` to `.env` and set values.
2. `npm install`
3. `npm run dev`
